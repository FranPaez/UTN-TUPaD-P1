# Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza:

numeros = [8, 15, 3, 22, 7]     # Crea una lista llamada numeros y le asigna valores
numeros.remove(max(numeros))    # Elimina el valor más alto
print(numeros)                  # Imprime la nueva lista de numeros